  pipeline{
    agent any
    stages{
        stage("SCM Checkout"){
            steps{
              echo '> coppying  the file ...'
             ansiblePlaybook credentialsId: 'private-key', disableHostKeyChecking: true, installation: 'ansible', inventory: 'inventory.in', playbook: 'myfirstplaybook.yml'
            }
        }
    }
}
