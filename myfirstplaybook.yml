- hosts: local
  vars:
    src_file: 
      - info0.txt
      - info1.txt
      - info2.txt
  tasks:
  - name: Ansible copy file to remote server
    copy:
      src: ~/ansibleproj/{{ item }}
      dest: ~/kartik
    loop: "{{src_file}}"
